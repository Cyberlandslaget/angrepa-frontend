export enum HomePanelEnum {
  Simple = 'simple',
  Runner = 'runner',
  Submission = 'submission',
}
export enum DragDirection {
  Row = 'row',
  Column = 'col',
}
export enum ExtendedType {
  Team = 'team',
  Service = 'service',
}
